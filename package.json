{
  "name": "water-use-simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "tsc --noEmit && node scripts/validate-data.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.7.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "three": "^0.186.1"
  }
}
